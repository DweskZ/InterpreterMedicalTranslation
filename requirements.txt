# Audio loopback (Windows WASAPI) + ASR + offline MT (EN->ES)
faster-whisper>=1.0.3
PyAudioWPatch>=0.2.12
numpy>=1.24.0
scipy>=1.10.0
argostranslate>=1.9.6
# Windows + GPU: runtime CUDA 12
nvidia-cublas-cu12>=12.1.0; platform_system == "Windows"
nvidia-cuda-runtime-cu12>=12.1.0; platform_system == "Windows"
